#pragma once

enum class AAspect {
    a,
    _end
};

#include "_gen_BAspectName.h"

enum class BAspect {
    b,
    _end
};
#include "_gen_BAspectName.h"
enum class CAspect {
    c,
    _end
};

enum class Aspect {
    foo,
    _end
};// what?
